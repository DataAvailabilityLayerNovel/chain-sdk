syntax = "proto3";
package evnode.v1;

option go_package = "github.com/DataAvailabilityLayerNovel/chain-sdk/types/pb/evnode/v1";

// Batch is a collection of transactions.
message Batch {
  repeated bytes txs = 1;
}
